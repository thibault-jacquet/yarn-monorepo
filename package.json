{
  "name": "yarn-monorepo",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "main": "index.js",
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -pt run build",
    "build:all": "lerna run build",
    "publish:all": "lerna publish --conventional-commits -y"
  },
  "devDependencies": {
    "lerna": "^3.22.1",
    "ts-loader": "^8.0.11",
    "typescript": "^4.1.2",
    "webpack": "^5.6.0",
    "webpack-cli": "^4.2.0"
  }
}
